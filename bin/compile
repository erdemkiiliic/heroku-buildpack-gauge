#!/usr/bin/env bash

echo "-----> Installing Gauge (latest)"

# change to the the BUILD_DIR ($1)
cd $1

# make a directory to gauge
mkdir -p app/gauge


# Install Gauge to /app/gauge
curl -Ssl https://downloads.gauge.org/stable | sh -- --location=/app/gauge

# untar the binary to the directory we want
# tar -C vendor/gauge -xvf gauge-1.4.3.tar.gz
